{
  "logs-[NAME]-unpacker": {
    "processors": [
      {
        "json": {
          "field": "message",
          "target_field": "parsed"
        }
      },
      {
        "script": {
          "lang": "painless",
          "source": "if (ctx.parsed != null) { for (entry in ctx.parsed.entrySet()) { if (!entry.getKey().equals('@timestamp')) { ctx[entry.getKey()] = entry.getValue(); } } ctx.remove('parsed'); }"
        }
      }
    ]
  }
}
